import styled from "styled-components";
import { colors } from "../../../utils/styles/colors";

export const HomeTeamWrapper = styled.section`
    width: 100%;
    height: 25%;
    background-color: ${colors.primary};
`